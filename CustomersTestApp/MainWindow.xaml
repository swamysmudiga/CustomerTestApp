<Window x:Class="CustomersTestApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:CustomersTestApp.ViewModels"
        Title="Customer Editor" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <ListBox Grid.Column="0" ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Name}" />
                        <TextBlock Text="{Binding Email}" />
                        <TextBlock Text="{Binding Discount}" />
                        <Button Content="Remove" Command="{Binding DataContext.RemoveCustomerCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Grid.Column="1">
            <TextBlock Text="Customer Details" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="Name:" />
            <TextBox Text="{Binding SelectedCustomer.Name, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text="Email:" />
            <TextBox Text="{Binding SelectedCustomer.Email, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text="Discount:" />
            <TextBox Text="{Binding SelectedCustomer.Discount, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text="ID:" />
            <TextBox Text="{Binding SelectedCustomer.Id}" IsReadOnly="True" />
            <Button Content="Save" Command="{Binding SaveCustomerCommand}" />
            <Button Content="Add New Customer" Command="{Binding AddCustomerCommand}" Margin="0,10,0,0" />
        </StackPanel>
    </Grid>
</Window>